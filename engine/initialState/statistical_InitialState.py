"""
Este módulo se encarga de determinar el inicio, día 0, de la simulación

Es necesario para poder calcular ciertos datos del día 1 y sus siguientes 

En este se especificarán los atributos del día weather y weekDay

Voy a tener en cuenta que al inicio la simulación solo funicona en el añ0 2017, en Navarra
"""


def initialWeather(context_dict): 
  temporality = context_dict.get("temporality", None)
  return #weather

def initialWeekDay(context_dict):
  return #weekDay

def initialDay(context_dict):
  return 

def calculate_weather(temporality):
  return